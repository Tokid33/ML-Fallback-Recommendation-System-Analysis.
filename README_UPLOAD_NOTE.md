# Upload note

Copy the folder `cases/traveltech-recommendation-fallback/` into your GitHub portfolio repository.

Optional link for the main repository README:

```markdown
- [TravelTech Recommendation Fallback Case](cases/traveltech-recommendation-fallback/) - fallback scenario for ML recommendations with API contract, UX switching, metrics and acceptance criteria.
```
